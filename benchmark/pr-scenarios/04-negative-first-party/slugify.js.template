/**
 * PR SCENARIO S4 — copy to src/lib/slugify.js (first-party, should NOT match Catalog)
 */
export function slugify(input) {
  return String(input)
    .trim()
    .toLowerCase()
    .replace(/[^a-z0-9]+/g, "-")
    .replace(/^-+|-+$/g, "");
}
