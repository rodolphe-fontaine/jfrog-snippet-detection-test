Native C helpers are vendored under `snippets/exact/ffmpeg-avstring.c` and linked from build scripts in real deployments.
This benchmark keeps the source file in-tree for snippet-level license detection (LGPL/GPL).
